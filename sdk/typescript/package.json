{
  "name": "@acp/sdk",
  "version": "0.1.0",
  "description": "Agent Consent Protocol SDK — 2FA for AI Agents. Zero external dependencies.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsx --test test/*.test.ts",
    "lint": "eslint src/"
  },
  "keywords": ["ai", "agent", "consent", "authorization", "2fa", "human-in-the-loop", "mcp"],
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/express": "^5.0.0",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0"
  },
  "peerDependencies": {
    "express": ">=4.0.0"
  },
  "peerDependenciesMeta": {
    "express": { "optional": true }
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
